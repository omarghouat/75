import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.omarghouat.app75',
  appName: '75',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
